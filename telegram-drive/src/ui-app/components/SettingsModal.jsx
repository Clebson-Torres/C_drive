import React from "react";
import { chunkSizeLabel, fmtSize } from "../lib/format";

export default function SettingsModal({ t, locale, open, settings, onChunkSizeChange, onParallelismChange, onEncryptChange, onSave, onClose }) {
  if (!open || !settings) return <div id="settingsModal" className="hidden" />;
  return <div id="settingsModal" className="modal-shell"><div className="modal-card max-w-2xl"><div className="flex items-start justify-between gap-4"><div><div className="text-xs font-semibold uppercase tracking-[0.26em] text-sky-700">{t("settings.badge")}</div><h3 className="mt-2 text-2xl font-semibold text-slate-950">{t("settings.title")}</h3></div><button id="btnSettingsClose" type="button" className="ghost-btn !px-4 !py-2" onClick={onClose}>{t("settings.close")}</button></div><div className="mt-6 space-y-5"><label className="block text-sm font-medium text-slate-700">{t("settings.chunk")}<select id="settingsChunkSize" value={String(settings.chunk_size_bytes)} onChange={(event) => onChunkSizeChange(Number(event.target.value))} className="mt-2 w-full rounded-2xl border border-slate-200 bg-white px-4 py-3"><option value={64 * 1024 * 1024}>64 MiB</option><option value={128 * 1024 * 1024}>128 MiB</option><option value={256 * 1024 * 1024}>256 MiB</option></select></label><p id="settingsChunkSummary" className="text-sm text-slate-600">{t("settings.chunkSummary", { chunkSize: chunkSizeLabel(settings.chunk_size_bytes) })}</p><label className="block text-sm font-medium text-slate-700">{t("settings.parallelism")}<input id="settingsParallelism" type="number" min="1" max="64" value={settings.max_parallelism} onChange={(event) => onParallelismChange(Number(event.target.value))} className="mt-2 w-full rounded-2xl border border-slate-200 bg-white px-4 py-3" /></label><label className="flex items-center gap-3 rounded-2xl border border-slate-200 bg-white px-4 py-3"><input id="settingsEncrypt" type="checkbox" checked={Boolean(settings.encrypt_chunks)} onChange={(event) => onEncryptChange(event.target.checked)} /><span className="text-sm text-slate-700">{t("settings.encrypt")}</span></label><div className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-4 text-sm text-slate-600"><div id="settingsDownloadCacheSummary">{t("settings.downloadCacheSummary", { threshold: fmtSize(settings.download_cache_threshold_bytes, locale) })}</div><div id="settingsDownloadCacheWriteSummary" className="mt-2">{t("settings.downloadCacheWriteSummary")}</div></div></div><div className="mt-8 flex justify-end"><button id="btnSettingsSave" type="button" className="primary-btn" onClick={onSave}>{t("settings.save")}</button></div></div></div>;
}
