import React from "react";
import { fmtSize } from "../lib/format";

export default function DownloadModal({ t, locale, open, file, settings, cacheMode, summary, onChangeMode, onConfirm, onCancel }) {
  if (!open || !file) return <div id="downloadModal" className="hidden" />;
  return <div id="downloadModal" className="modal-shell"><div className="modal-card max-w-xl"><div className="flex items-start justify-between gap-4"><div><div className="text-xs font-semibold uppercase tracking-[0.26em] text-sky-700">{t("download.badge")}</div><h3 className="mt-2 text-2xl font-semibold text-slate-950">{file.name}</h3></div><button id="btnDownloadCancel" type="button" className="ghost-btn !px-4 !py-2" onClick={onCancel}>{t("download.close")}</button></div><div id="downloadTargetSummary" className="mt-5 rounded-2xl border border-slate-200 bg-slate-50 px-4 py-4 text-sm text-slate-700">{t("download.targetSummary", { size: fmtSize(file.size, locale), threshold: fmtSize(settings?.download_cache_threshold_bytes, locale) })}</div><label className="mt-5 block text-sm font-medium text-slate-700">{t("download.cachePolicy")}<select id="downloadCacheMode" value={cacheMode} onChange={(event) => onChangeMode(event.target.value)} className="mt-2 w-full rounded-2xl border border-slate-200 bg-white px-4 py-3"><option value="default">{t("download.optionDefault")}</option><option value="disabled">{t("download.optionDisabled")}</option><option value="enabled">{t("download.optionEnabled")}</option></select></label><p id="downloadCacheSummary" className="mt-4 text-sm text-slate-600">{summary}</p><div className="mt-8 flex justify-end gap-3"><button type="button" className="ghost-btn" onClick={onCancel}>{t("download.cancel")}</button><button id="btnDownloadConfirm" type="button" className="primary-btn" onClick={onConfirm}>{t("download.confirm")}</button></div></div></div>;
}
